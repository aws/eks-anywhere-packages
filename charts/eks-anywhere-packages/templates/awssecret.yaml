apiVersion: v1
data:
  {{- with .Values.awsSecret }}
  ID: "{{ .id | b64enc }}"
  SECRET: "{{ .secret | b64enc }}"
  REGION: "{{ .region | b64enc }}"
  {{- end }}
kind: Secret
metadata:
  name: aws-secret
  namespace: {{ .Values.namespace }}
type: Opaque
