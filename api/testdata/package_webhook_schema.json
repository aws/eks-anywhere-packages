{
  "$id": "https://hello-eks-anywhere.packages.eks.amazonaws.com/schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "hello-eks-anywhere",
  "type": "object",
  "required": [
    "subtitle",
    "nested.prop.test1",
    "nested.prop.test2"
  ],
  "properties": {
    "sourceRegistry": {
      "type": "string",
      "default": "public.ecr.aws/eks-anywhere",
      "description": "Source registry for package."
    },
    "subtitle": {
      "type": "string",
      "default": "curated-packages",
      "description": "Source registry for package."
    },
    "title": {
      "type": "string",
      "default": "Amazon EKS Anywhere",
      "description": "Container title."
    },
    "nested.prop.test1": {
      "type": "string",
      "default": "nested property test1",
      "description": "Container title."
    },
    "nested.prop.test2": {
      "type": "string",
      "default": "nested property test2",
      "description": "Container title."
    }
  },
  "additionalProperties": false
}
