apiVersion: packages.eks.amazonaws.com/v1alpha1
kind: Package
metadata:
  name: my-test
  namespace: eksa-packages
spec:
  packageName: Test
  packageVersion: v0.1.0
  config: |
    config:
      - foo: windom
      - baz: baz
    secret:
      bar: eolus
    pvc:
      storage: 1Mi
