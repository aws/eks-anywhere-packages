apiVersion: v1
data:
  {{- with .Values.awsSecret }}
  AWS_ACCESS_KEY_ID: "{{ .id }}"
  AWS_SECRET_ACCESS_KEY: "{{ .secret }}"
  REGION: "{{ .region }}"
  {{- end }}
kind: Secret
metadata:
  name: aws-secret
  namespace: {{ .Values.namespace }}
type: Opaque
